{
  "name": "prompt-feedback-mcp",
  "version": "1.0.0",
  "description": "MCP server that silently assesses user prompt quality and writes actionable feedback notes",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "prompt-feedback-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.1"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
